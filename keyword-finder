#!/usr/bin/env python3
"""
Entry point script for the keyword-finder command.
This makes the tool available as `keyword-finder` command globally.
"""

import sys
import os

# Add src to path for imports
sys.path.insert(0, os.path.join(os.path.dirname(__file__), 'src'))

from src.platform.cli import main

if __name__ == "__main__":
    sys.exit(main())